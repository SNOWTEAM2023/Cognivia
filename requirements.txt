# [FIXED] PR review: requirements.txt was referenced by the README but never committed, so
# `pip install -r requirements.txt` failed at the very first quickstart step.
# Dependencies below come from the imports actually used by the six scripts:
# - identify_distortion.py / generate_response.py
# - fine-tuned_model_generate.py
# - evaluation_with_NLP.py / evaluation_with_CogEval_criteria.py / evaluate_with_existing_criteria.py
#
# openai must stay >=1.x: the code imports RateLimitError/APIConnectionError/etc. from the top-level
# package and uses the object-style client API (OpenAI(...).chat.completions.create). Both require
# the newer openai 1.x API surface, so the dependency must not be pinned below v1.

openai>=1.0.0
pandas>=1.3
numpy>=1.21
openpyxl>=3.0
requests>=2.28
nltk>=3.8
rouge-score>=0.1.2
sacrebleu>=2.3.1
scikit-learn>=1.1
tqdm>=4.64
